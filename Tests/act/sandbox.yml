#This is a dummy workflow file usually run from act
name: Sandbox
on:
  push:
    branches:
      - sandbox
jobs:
  localTest:
    runs-on: ubuntu-latest
    steps:
      - name: Super Duper Linter Test
        uses: ./Tests/act
        with:
          path: Tests/Mocks/codeToLint
          excludePath: 
          linter: markdown
          include: 
          debug: true
          parallel: 8
          problemsonly: true
        continue-on-error: true