#This is a dummy workflow file usually run from act
name: Sandbox
on:
  push:
    branches:
      - sandbox
jobs:
  localTest:
    runs-on: ubuntu-latest
    steps:
      - name: Super Duper Linter Test
        uses: ./Tests/act
        with:
          path: Tests/Mocks/codeToLint/test
          debug: true
        continue-on-error: true