name: 'Super Duper Linter Test'
description: 'Lints and validates code files in your repository, very helpful for Pull Requests!'
inputs:
  name:
    description: Name(s) of the linters you wish to run. Runs all by default
    required: false
  path:
    description: Name(s) of the linters you wish to run. Runs all by default
    required: false
  debug:
    description: Set to True to enable advanced Powershell-level debugging
    required: false
# outputs:
#   time: # id of output
#     description: 'The time we greeted you'
runs:
  using: 'docker'
  #This is generated in the Github Action Workflow by the docker-build-with-cache-action@v5 action that should be called prior to this one. Its name is hard-coded for now
  image: 'my_awesome_image'
branding:
  icon: check-circle
  color: purple